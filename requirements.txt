# Core renderer
manim>=0.19.0

# Dev / tests
pytest>=7.0

# Deepgram TTS uses the Python standard library (urllib) — no SDK required.
# The offline fallback (estimated-silence narration) is also standard library.
# FFmpeg must be installed and on PATH (used for muxing).
